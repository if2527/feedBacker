const baseUrl = 'http://localhost:8000/api/v1';

export const API_URLS = {
  REGIONS: `${baseUrl}/region`,
  CITIES: `${baseUrl}/city`,
  FEEDBACK: `${baseUrl}/feedback`,
  FILTERED_CITIES: `${baseUrl}/city/region`
}
